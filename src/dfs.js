// Returns Path Array
const DFS = (currentNode, endNode) => {
    if (
        currentNode.xCoord === endNode.xCoord &&
        currentNode.yCoord === endNode.yCoord
    ) {
        // Path Found
    }

}